[project]
name = "diana"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "RiccardoMarantonio", email = "riccardo.marantonio@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "torch>=2.13.0",
    "torchvision>=0.28.0",
]

[project.scripts]
diana = "diana:main"

[build-system]
requires = ["uv_build>=0.8.9,<0.9.0"]
build-backend = "uv_build"
